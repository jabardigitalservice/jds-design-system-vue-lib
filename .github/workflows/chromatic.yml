name: 'Chromatic UI Test'

on:
  pull_request:
    branches:
      - master
      - 'setup/chromatic'
jobs:
  chromatic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          npm install # install dependencies
      - run: |
          npm run chromatic --project-token=${{ secrets.CHROMATIC_TOKEN }} # publish Storybook and run visual tests
